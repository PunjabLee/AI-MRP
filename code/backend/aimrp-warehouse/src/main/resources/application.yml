# AI MRP 仓库服务配置
spring:
  application:
    name: aimrp-warehouse
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:standalone}
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:aimrp}
    username: ${DB_USER:postgres}
    password: ${DB_PASSWORD:postgres}
  config:
    import: optional:file:./application-${spring.profiles.active}.yml

mybatis-plus:
  mapper-locations: classpath*:/mapper/**/*.xml
  type-aliases-package: com.aimrp.warehouse.infrastructure.persistence.po
  configuration:
    map-underscore-to-camel-case: true

# 默认单体模式配置
server:
  port: ${SERVER_PORT:8091}

spring:
  cloud:
    discovery:
      enabled: false

# Actuator 配置
management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
  endpoint:
    health:
      show-details: always
