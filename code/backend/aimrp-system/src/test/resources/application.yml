spring:
  datasource:
    url: jdbc:h2:mem:testdb
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: false

jwt:
  secret: test-secret-key-for-jwt-token-generation-must-be-long-enough-32chars
  expiration: 86400000

logging:
  level:
    com.aimrp: INFO
